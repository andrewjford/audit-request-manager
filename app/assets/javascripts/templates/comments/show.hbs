<li>
  <div>
    <a href="/users/{{user.id}}">{{user.name}}</a>: {{content}}

    {{#if}}
    <a href="/project/x/request/y/comment/{{id}}" method="delete" class="small-link 
    float-right">delete</a>
  </div>
  <i>Posted on <%= post_date(comment) %></i>
</li>

if policy(comment).destroy?
  link_to "delete",
    project_request_comment_path(comment.request.project, comment.request, comment),
    method: :delete,
    class: "small-link float-right"
end
